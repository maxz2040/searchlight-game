# Logs
logs
*.log
npm-debug.log*
yarn-debug.log*
yarn-error.log*
pnpm-debug.log*
lerna-debug.log*

node_modules
dist
dist-ssr
*.local

# Editor directories and files
.vscode/*
!.vscode/extensions.json
.idea
.DS_Store
*.suo
*.ntvs*
*.njsproj
*.sln
*.sw?

# Playwright
test-results/
playwright-report/

# Report bundle: regenerable artefacts (PDF + HTML + screenshots, ~98 MB).
report/

# Local secrets — never commit
.env
.env.local
scratch/


# Model comparison doc — bundles 4×2.5 MB inlined PNGs (28 MB total).
# Regenerate with: npx tsx scripts/build-model-comparison.ts
docs/model-comparison.html

# Scene verification screenshots — auto-regenerable via scratch/verify-scenes.ts
docs/v1-scene-verify/

# UAT and impeccable-critique screenshot runs — auto-regenerable via the
# capture/UAT scripts; ~94 MB combined, not shipped to production.
docs/impeccable-critique/
docs/v1-uat/
docs/v2-uat/

# Character reference sheets (~4-5MB each × 50 = 215MB, regenerable via
# scratch/sheets/dispatch.sh). Used as compositing inputs only — not
# shipped to production.
public/character-sheets/
